---
title: 'Turbopack และ Build Performance'
description: 'Turbopack bundler ที่เขียนด้วย Rust และตอนนี้เป็น default ของ next dev และ next build — incremental compilation, HMR และ cold start ที่เร็วขึ้น, ต่างจาก webpack ยังไง และ build caching'
sidebar:
  order: 4
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## ไอเดียหลักในหนึ่งประโยค

Turbopack คือ bundler ของ Next.js ที่เขียนด้วย Rust ตอนนี้เป็น default ทั้ง `next dev` และ `next build` และเร่ง feedback loop ด้วยการ compile แบบ incremental แทนที่จะ rebuild ทุกอย่างใหม่

## Turbopack เป็น default แล้ว

ใน Next.js ปัจจุบัน (เวอร์ชัน 16 ณ ตอนที่เขียน) Turbopack เป็น bundler default ทั้ง `next dev` และ `next build` ฝั่ง development กับ Turbopack นิ่งมาระยะหนึ่งแล้ว ส่วน path ของ production build เป็นชิ้นที่ใหม่กว่า ควรเช็กกับ docs ของเวอร์ชันที่คุณใช้จริง คุณไม่ต้องใส่ flag อะไรเพื่อเปิดใช้ — script ธรรมดาก็พอ

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  }
}
```

ถ้าเจอปัญหาแล้วต้องกลับไปใช้ bundler เดิม คุณ opt กลับไปใช้ webpack ได้เป็นราย command

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build --webpack",
    "start": "next start"
  }
}
```

`next.config` ที่มีฟังก์ชัน `webpack()` แบบ custom แต่ไม่มี key `turbopack` จะทำให้ `next build` แบบ default **fail** แทนที่จะเงียบ ๆ ข้ามไป — ใส่ `--webpack` เพื่อใช้ webpack ต่อ หรือ `--turbopack` เพื่อให้ข้าม webpack config ไป

## Turbopack เร่งอะไรบ้าง

แก่นคือ incremental compilation Turbopack สร้าง dependency graph แล้วคำนวณใหม่เฉพาะส่วนที่ได้รับผลจากการเปลี่ยนแปลง cold start จึงเร็วขึ้นและ Hot Module Replacement ยังเกือบทันทีแม้แอปจะโตขึ้น เมื่อคุณ save ไฟล์ Turbopack จะ recompile เฉพาะ module นั้นกับตัวที่ขึ้นกับ module นั้น ไม่ใช่ทั้ง route

## ต่างจาก webpack ยังไง

webpack คือ JavaScript bundler ที่สุกงอมและมี plugin ecosystem มหาศาล แต่แก่น JavaScript และแนวโน้มที่จะ rebuild ทั้งก้อนกลายเป็นคอขวดในแอปใหญ่ Turbopack เขียนด้วย Rust ออกแบบมาให้ทำงาน multi-core และถือว่างาน incremental แบบละเอียดเป็น default ไม่ใช่ของเสริม plugin และ loader ของ webpack บางตัวใช้ไม่ได้ — Next.js มีตัวเทียบเท่าให้เอง และคุณตั้งค่า Turbopack ใต้ key `turbopack`

```ts
// next.config.ts — Turbopack-specific configuration
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  turbopack: {
    // e.g. custom rules for importing SVGs as components
    rules: {},
  },
}

export default nextConfig
```

## build caching

Turbopack เก็บงานลง disk ได้ การ start `dev` หรือ `build` ครั้งถัดไปจึงใช้ผล compile เดิมซ้ำแทนที่จะเริ่มจากศูนย์ cache ทั้งสองตัว**เปิดโดย default** (dev ตั้งแต่ 16.1, build ตั้งแต่ 16.3) แม้ option จะยังอยู่ใต้ `experimental` ก็ตาม cache ฝั่ง build อยู่ที่ `.next/cache/turbopack` จึงช่วยได้ก็ต่อเมื่อ CI ของคุณ restore `.next/cache` ระหว่าง build — เซ็ต option เป็น `false` เพื่อ opt out

```ts
// next.config.ts — both caches are on by default; opt out per command
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    turbopackFileSystemCacheForDev: true, // default: true (16.1+)
    turbopackFileSystemCacheForBuild: false, // default: true (16.3+); false if CI never restores .next/cache
  },
}

export default nextConfig
```

<Mermaid title="incremental compilation ใช้งานที่ไม่เปลี่ยนซ้ำ" code={diagramTurboTh} />

export const diagramTurboTh = `flowchart LR
  Save[Save a file] --> Graph[Dependency graph]
  Graph --> Changed[Recompile changed module + dependents]
  Graph --> Cache[Reuse unchanged from cache]
  Changed --> HMR[Fast HMR / build]
  Cache --> HMR`;

<Callout title="สรุปหนึ่งบรรทัด">
Turbopack คือ bundler ที่เขียนด้วย Rust ทำงาน incremental และเป็น default แล้วของ `next dev` และ `next build` — cold start และ HMR เร็วขึ้น ใช้ `--webpack` เพื่อ opt out และมี filesystem cache ที่เปิดโดย default ทั้ง dev และ build
</Callout>

export const quizTurboTh = [
  {
    q: 'Turbopack เขียนด้วยภาษาอะไร',
    options: [
      'JavaScript',
      'Go',
      'Rust',
      'C++',
    ],
    answer: 2,
  },
  {
    q: 'ใน Next.js ปัจจุบัน Turbopack เป็น default bundler ของอะไร',
    options: [
      'เฉพาะ next dev',
      'เฉพาะ next build',
      'ทั้ง next dev และ next build',
      'ไม่ใช่ทั้งคู่ ต้อง opt-in เอง',
    ],
    answer: 2,
  },
  {
    q: 'จะ opt กลับไปใช้ webpack สำหรับ production build ยังไง',
    options: [
      'ลบ next.config ทิ้ง',
      'สั่ง next build --webpack',
      'เซ็ต NODE_ENV=webpack',
      'ทำไม่ได้',
    ],
    answer: 1,
  },
  {
    q: 'อะไรทำให้ Turbopack เร็วเมื่อทำงานซ้ำ',
    options: [
      'rebuild ทั้งแอปทุกครั้ง',
      'incremental compilation บวกกับ filesystem cache',
      'ข้ามการ type-check',
      'รันบน core เดียว',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="optimization/turbopack-and-build-performance" questions={quizTurboTh} />

<ProgressTracker client:visible id="optimization/turbopack-and-build-performance" />
