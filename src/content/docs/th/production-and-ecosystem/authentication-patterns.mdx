---
title: Pattern การทำ Authentication
description: session และ cookie ทำ optimistic check ใน proxy แต่ทำ authorization จริงใน Data Access Layer และทำไม proxy อย่างเดียวไม่พอ
sidebar:
  order: 2
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## ไอเดียในหนึ่งประโยค

ทำ optimistic check เบา ๆ ใน proxy เพื่อ redirect request ที่ไม่ auth ชัด ๆ ออกไป แต่ทำ authorization จริงให้ใกล้ data ที่สุด ใน Data Access Layer ที่ verify session ก่อนจะ return อะไรออกมา

## Session และ cookie

auth ส่วนใหญ่วิ่งอยู่บน session ที่เก็บใน cookie แบบ HTTP-only ฝั่ง server เราอ่าน cookie นั้นด้วย `cookies()` จาก `next/headers`

```ts
// app/lib/session.ts — read the session cookie on the server
import { cookies } from 'next/headers'
import { decrypt } from '@/app/lib/crypto'

export async function getSession() {
  const cookie = (await cookies()).get('session')?.value
  if (!cookie) return null
  return decrypt(cookie)
}
```

## Optimistic check ใน proxy

proxy (`proxy.ts`) เป็นที่ที่เหมาะกับ gate แบบ optimistic ราคาถูก ถ้าไม่มี session ชัด ๆ ก็ redirect ก่อน render วิธีนี้เร็วก็จริง แต่แค่ redirect ห้ามใช้เป็นด่านเดียว

```ts
// proxy.ts — optimistic redirect only
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

const protectedRoutes = ['/dashboard']

export function proxy(request: NextRequest) {
  const path = request.nextUrl.pathname
  const hasSession = request.cookies.has('session')
  if (protectedRoutes.some((p) => path.startsWith(p)) && !hasSession) {
    return NextResponse.redirect(new URL('/login', request.url))
  }
  return NextResponse.next()
}
```

## Authorization ตัวจริงอยู่ใน Data Access Layer

proxy อย่างเดียวไม่พอ `matcher` ที่ exclude path ไหนก็จะข้าม Server Action call บน path นั้นไปด้วย layout ก็ไม่ได้ re-run ทุกครั้งที่ navigate และ check ที่อยู่แค่หน้า app อาจถูก bypass ได้ทั้งดุ้น [CVE-2025-29927](https://github.com/advisories/GHSA-f82v-jwr5-mffw) เปิดให้ผู้โจมตีข้าม middleware ทั้งหมดด้วย header `x-middleware-subrequest` ที่ปลอมขึ้นมา (แก้แล้วใน Next.js 15.2.3) ทางแก้คือ Data Access Layer (DAL) จุดเดียวที่ verify session ก่อน return data ห่อด้วย React `cache` เพื่อให้รันครั้งเดียวต่อ request

```ts
// app/lib/dal.ts — verify the session close to the data
import 'server-only'
import { cache } from 'react'
import { redirect } from 'next/navigation'
import { getSession } from '@/app/lib/session'

export const verifySession = cache(async () => {
  const session = await getSession()
  if (!session?.userId) {
    redirect('/login')
  }
  return { isAuth: true, userId: session.userId }
})
```

จากนั้นทุก page, Server Action หรือ route handler ที่แตะ data ที่ป้องกันไว้จะเรียก `verifySession()` ก่อน สำหรับโปรเจกต์จริง library อย่าง NextAuth (Auth.js), Clerk หรือ Better Auth จัดการ session, provider และ token ให้ แต่ pattern DAL สำหรับ authorization ก็ยังต้องวางทับ library พวกนี้อยู่ดี

<Mermaid title="gate แบบ optimistic ใน proxy, check ตัวจริงใน DAL" code={diagramAuthTh} />

export const diagramAuthTh = `
graph TD
  A["Request"] --> B["proxy: optimistic redirect"]
  B --> C["Page / Server Action"]
  C --> D["verifySession() in DAL"]
  D -->|no session| E["redirect to /login"]
  D -->|valid| F["Return protected data"]
`;

<Callout title="สรุปในหนึ่งบรรทัด">
ใช้ proxy ทำ optimistic redirect เร็ว ๆ แต่วาง authorization check ตัวจริงไว้ใน Data Access Layer ที่ verify session ก่อน return data
</Callout>

export const quizAuthTh = [
  {
    q: 'อ่าน session cookie ฝั่ง server ได้อย่างไร',
    options: [
      'document.cookie',
      'cookies() จาก next/headers',
      'localStorage.getItem',
      'hook useCookie()',
    ],
    answer: 1,
  },
  {
    q: 'proxy ควรทำอะไรสำหรับ auth',
    options: [
      'รัน authorization check แบบเต็ม',
      'optimistic redirect เบา ๆ',
      'query database เพื่อเช็ค permission',
      'ไม่ทำอะไรเลย auth ใช้ proxy ไม่ได้',
    ],
    answer: 1,
  },
  {
    q: 'ทำไม proxy อย่างเดียวถึงไม่พอสำหรับ authorization',
    options: [
      'มันอ่าน cookie ไม่ได้',
      'matcher อาจข้ามบาง path, layout ไม่ re-run ทุกครั้งที่ navigate และมันถูก bypass ได้',
      'มันรันแค่ตอน development',
      'มัน redirect request ไม่ได้',
    ],
    answer: 1,
  },
  {
    q: 'authorization check ตัวจริงควรอยู่ตรงไหน',
    options: [
      'ใน Data Access Layer ที่อยู่ใกล้ data',
      'ใน proxy เท่านั้น',
      'ใน browser ก่อน fetch',
      'ใน root layout เท่านั้น',
    ],
    answer: 0,
  },
];

<Quiz client:visible id="production-and-ecosystem/authentication-patterns" questions={quizAuthTh} />

<ProgressTracker client:visible id="production-and-ecosystem/authentication-patterns" />
