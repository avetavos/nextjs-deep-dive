---
title: 'Route Segment Config และ Runtime'
description: 'Route Segment Config exports (dynamic, revalidate, runtime, fetchCache, dynamicParams) Node.js runtime กับ Edge runtime ที่ถูก deprecate แล้ว เปิด ISR ผ่าน revalidate ยังไง และทำไมทั้งหมดนี้ใช้กับ cacheComponents ไม่ได้'
sidebar:
  order: 4
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

<Callout title="บทนี้ใช้โมเดล caching แบบไหน">
Route Segment Config คือโมเดล caching **แบบเดิม** ที่ `cacheComponents` ปิดอยู่ พอตั้ง `cacheComponents: true` (บท Partial Prerendering และ `use cache`) ทุก export ในหน้านี้ — `dynamic`, `revalidate`, `fetchCache`, `dynamicParams` และ `runtime` (แม้แต่ `'nodejs'`) — จะทำให้ `next build` ล้มด้วย `Route segment config "…" is not compatible with nextConfig.cacheComponents. Please remove it.` ตัวแทน — ตัด `force-dynamic` ทิ้ง (route เป็น dynamic โดย default อยู่แล้ว) แทน `force-static` / `revalidate` / `fetchCache` ด้วย `'use cache'` บวก `cacheLife(...)` (ดูด้านล่าง) แทน `dynamicParams = false` ด้วย `notFound()` ในหน้า page และเอา `runtime` ออกทั้งหมด — Cache Components ต้องใช้ Node.js
</Callout>

## ไอเดียในหนึ่งประโยค

Route Segment Config คือชุดของค่า `export const` ระดับไฟล์ ที่ให้คุณ override ว่า route segment เดียว render, cache และรันบน runtime ไหน

## segment config exports

แต่ละตัวเป็น named export อยู่บนสุดของ `layout.tsx`, `page.tsx` หรือ `route.ts` มีผลกับ segment นั้นและลูกของตัวเอง

```tsx
// app/blog/page.tsx — route segment config exports
export const dynamic = 'auto';       // 'auto' | 'force-dynamic' | 'error' | 'force-static'
export const revalidate = 3600;      // ISR: rebuild this route at most once per hour
export const runtime = 'nodejs';     // 'nodejs' (default) | 'edge' (deprecated)
export const fetchCache = 'auto';    // control the default caching of fetch() in this segment
export const dynamicParams = true;   // allow params not returned by generateStaticParams
```

- **`dynamic`** — บังคับ mode ของการ render `'force-dynamic'` render ทุก request ส่วน `'force-static'` ห้ามใช้ dynamic API
- **`revalidate`** — จำนวนวินาทีระหว่างการ rebuild เบื้องหลัง นี่คือวิธีเปิด ISR
- **`runtime`** — สภาพแวดล้อมที่รัน `'nodejs'` (default) หรือ `'edge'` (deprecated ใน Next.js 16)
- **`fetchCache`** — override ว่า `fetch()` ใน segment นี้ถูก cache เป็น default ยังไง
- **`dynamicParams`** — เมื่อเป็น `false` param ที่ไม่อยู่ในลิสต์ของ generateStaticParams จะคืน 404 แทนที่จะ render ตอน request

## Node.js runtime กับ Edge runtime

ทุก route รันบน runtime default คือ **Node.js** ที่ให้ Node API เต็มชุด ส่วน **Edge** runtime เป็นสภาพแวดล้อมที่เล็กกว่าและมีแต่ Web API ตั้งแต่ Next.js 16 มันถูก **deprecate** ในฐานะ runtime ของ route — `next build` จะพิมพ์ `The Edge Runtime is deprecated` ให้ทุก page ที่ export มัน และ Cache Components ปฏิเสธ export นี้ไปเลย Edge ยังอยู่ต่อสำหรับ `proxy.ts` (ไฟล์ที่เดิมเรียกว่า Middleware)

```tsx
// Opt a route into the Edge runtime — deprecated since Next.js 16: next build warns, Cache Components rejects it
export const runtime = 'edge';
```

ในทางปฏิบัติหมายความว่า —

- **Node.js (default)** — Node API เต็ม, native module, library ใหญ่ ๆ, database driver และเป็น runtime เดียวที่รองรับ ISR กับ Cache Components ไม่ต้อง export `runtime` เลยก็ได้ตัวนี้
- **Edge** — ชุด API จำกัด (ไม่มี filesystem, ไม่มี native module, ไม่มี `require`) ไม่มี ISR และมี deprecation warning ตอน build ถ้าต้องการ logic แบบ edge เช่น redirect หรือ auth check เบา ๆ ให้ไปไว้ใน `proxy.ts` แทนที่จะอยู่บน route

## ISR ผ่าน revalidate

Incremental Static Regeneration ทำให้ route ยัง static และเร็ว แต่ยอมให้ content stale ได้แล้ว refresh ตามเวลา ตั้ง `revalidate` เป็นจำนวนวินาที Next.js จะเสิร์ฟ static page ที่ cache ไว้ แล้ว rebuild เบื้องหลังหลังพ้นช่วงนั้น

```tsx
// app/products/page.tsx — serve static, refresh in the background every 60s
export const revalidate = 60;

export default async function Products() {
  const products: { id: number; name: string }[] = await fetch('https://api.example.com/products').then((r) => r.json());
  return <ul>{products.map((p) => <li key={p.id}>{p.name}</li>)}</ul>;
}
```

request แรกหลังพ้น 60 วินาทีจะ trigger การ rebuild เบื้องหลัง ผู้เข้าชมยังได้ page ที่ cache ไว้แบบเร็วต่อไปจนกว่าอันใหม่จะพร้อม

ภายใต้ `cacheComponents: true` export `revalidate` จะถูกปฏิเสธ ตัวแทนคือ scope `'use cache'` พร้อม profile ของ `cacheLife` — จากนั้น `next build` จะแสดง route เป็น `○ /products  1m  1h` —

```tsx
// app/products/page.tsx — the same ISR under Cache Components: cache the page and give it a lifetime
import { cacheLife } from 'next/cache';

export default async function Products() {
  'use cache';
  cacheLife('minutes'); // stale 5m, revalidate 1m, expire 1h — closest preset to revalidate = 60
  const products: { id: number; name: string }[] = await fetch('https://api.example.com/products').then((r) => r.json());
  return <ul>{products.map((p) => <li key={p.id}>{p.name}</li>)}</ul>;
}
```

<Mermaid title="Route segment config กับ runtime ทั้งสอง" code={diagramSegmentTh} />

export const diagramSegmentTh = `flowchart TD
  A[Route segment config] --> B[dynamic]
  A --> C[revalidate for ISR]
  A --> D[runtime]
  D --> E[nodejs, full APIs, default]
  D --> F[edge, deprecated in 16, limited APIs, no ISR]`;

<Callout title="สรุปในหนึ่งบรรทัด">
Route Segment Config exports ให้ segment เดียว override การ render, caching และ runtime ได้ในโมเดลแบบเดิม — revalidate เปิด ISR, runtime ควรอยู่ที่ Node.js ตัว default เพราะ Edge ถูก deprecate แล้ว และภายใต้ cacheComponents export ทั้งหมดนี้ต้องเปลี่ยนไปใช้ `'use cache'` บวก cacheLife
</Callout>

export const quizSegmentTh = [
  {
    q: 'ค่า Route Segment Config อยู่ที่ไหน?',
    options: ['ใน next.config เท่านั้น', 'เป็น named export บนสุดของไฟล์ segment', 'ในตัว component body', 'ในไฟล์ JSON แยกต่างหาก'],
    answer: 1,
  },
  {
    q: 'export ตัวไหนเปิด Incremental Static Regeneration?',
    options: ['runtime', 'dynamicParams', 'revalidate', 'fetchCache'],
    answer: 2,
  },
  {
    q: 'runtime default ของ route คืออะไร?',
    options: ['edge', 'nodejs', 'browser', 'deno'],
    answer: 1,
  },
  {
    q: 'สถานะของ export const runtime = \'edge\' ใน Next.js 16 คืออะไร?',
    options: ['เป็น runtime default', 'ถูก deprecate แล้ว — next build เตือน และ Cache Components ปฏิเสธ export นี้', 'จำเป็นต้องมีเพื่อเปิด ISR', 'เป็น runtime เดียวที่รองรับ use cache'],
    answer: 1,
  },
];

<Quiz client:visible id="rendering/route-segment-config-and-runtimes" questions={quizSegmentTh} />

<ProgressTracker client:visible id="rendering/route-segment-config-and-runtimes" />
